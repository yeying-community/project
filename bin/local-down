#!/bin/bash

set -euo pipefail

source "$(cd "$(dirname "$0")" && pwd)/local-common"

echo "./cmd local-down no longer stops shared middleware containers." >&2
echo "Manage MySQL/Redis/AppStore with your shared local environment instead." >&2
exit 1
