#!/bin/bash

set -euo pipefail

source "$(cd "$(dirname "$0")" && pwd)/local-common"

echo "./cmd local-up no longer starts MySQL/Redis/AppStore containers." >&2
echo "Start shared middleware outside this project and configure .env instead." >&2
exit 1
